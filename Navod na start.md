# 🚀 Інструкція запуску TAXA Platform

## ⚠️ ВАЖЛИВО: Перед запуском платформи

### Крок 1: Запустіть Docker Desktop
1. Знайдіть іконку **Docker Desktop** на робочому столі або в меню Пуск
2. Двічі клацніть, щоб запустити
3. **Зачекайте 30-60 секунд**, поки Docker повністю завантажиться
4. Іконка Docker в системному треї (біля годинника) стане **зеленою** ✅

### Крок 2: Запустіть платформу
Тепер можете запустити один з батників:
- **`Start All Services.bat`** - запускає всі сервіси автоматично
- **`Launch TAXA (secure).bat`** - запускає з налаштуванням OpenAI API ключа

---

## 🔧 Налаштування автозапуску Docker (опціонально)

Щоб Docker запускався автоматично при завантаженні Windows:

1. Відкрийте **Docker Desktop**
2. Натисніть на іконку ⚙️ (Settings) у верхньому правому куті
3. Перейдіть на вкладку **General**
4. Увімкніть опцію: ☑️ **"Start Docker Desktop when you log in"**
5. Натисніть **Apply & Restart**

Після цього Docker буде запускатися автоматично! 🎉

---

## 📋 Швидка діагностика

### Якщо платформа не запускається:

**Перевірте Docker:**
```powershell
docker ps
```

- ✅ Якщо показує список контейнерів → Docker працює
- ❌ Якщо помилка "cannot find the file" → Docker не запущений

**Перевірте статус сервісів:**
```powershell
docker compose ps
```

**Запустіть всі сервіси:**
```powershell
docker compose up -d
```

**Перегляньте логи:**
```powershell
docker compose logs -f
```

---

## 🌐 Доступ до платформи

Після успішного запуску:
- **Frontend:** http://localhost:3000
- **Backend API:** http://localhost:8000/docs
- **MinIO Console:** http://localhost:9001

---

## ❓ Часті питання

**Q: Чому платформа не працює після перезавантаження?**
A: Docker Desktop не запущений. Запустіть його вручну або налаштуйте автозапуск.

**Q: Як зупинити платформу?**
A: Запустіть `Stop TAXA.bat` або виконайте `docker compose down`

**Q: Як повністю перезібрати платформу?**
A: Запустіть `Fix & Rebuild.bat`
